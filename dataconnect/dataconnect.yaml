specVersion: "v1beta"
serviceId: "resumebuilde"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "resumebuilde-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
